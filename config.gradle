ext {
    def major = 1
    def minor = 2
    def patch = 1

    buildConfig = [
            "minSdk"    : 21,
            "compileSdk": 32,
            "targetSdk" : 32,
            "buildTools": "32.0.0"
    ]
    releaseConfig = [
            "group"      : "com.infinum.sentinel",
            "version"    : "$major.$minor.$patch",
            "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}