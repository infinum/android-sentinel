ext {
    def major = 1
    def minor = 3
    def patch = 1

    buildConfig = [
            "minSdk"    : 21,
            "compileSdk": 33,
            "targetSdk" : 33,
            "buildTools": "33.0.2"
    ]
    releaseConfig = [
            "group"      : "com.infinum.sentinel",
            "version"    : "$major.$minor.$patch",
            "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}