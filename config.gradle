ext {
    def major = 1
    def minor = 2
    def patch = 2

    buildConfig = [
            "minSdk"    : 21,
            "compileSdk": 33,
            "targetSdk" : 33,
            "buildTools": "33.0.0"
    ]
    releaseConfig = [
            "group"      : "com.infinum.sentinel",
            "version"    : "$major.$minor.$patch",
            "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}