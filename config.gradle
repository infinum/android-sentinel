ext {
    def major = 1
    def minor = 5
    def patch = 1

    buildConfig = [
            "minSdk"    : 23,
            "compileSdk": 36,
            "targetSdk" : 36,
            "buildTools": "35.0.0"
    ]
    releaseConfig = [
            "group"      : "com.infinum.sentinel",
            "version"    : "$major.$minor.$patch",
            "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}