ext {
    def major = 1
    def minor = 3
    def patch = 3

    buildConfig = [
            "minSdk"    : 21,
            "compileSdk": 34,
            "targetSdk" : 34,
            "buildTools": "34.0.0"
    ]
    releaseConfig = [
            "group"      : "com.infinum.sentinel",
            "version"    : "$major.$minor.$patch",
            "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}