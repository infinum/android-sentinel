ext {
    def major = 1
    def minor = 5
    def patch = 1

    buildConfig = [
            "minSdk"    : 21,
            "compileSdk": 35,
            "targetSdk" : 35,
            "buildTools": "34.0.0"
    ]
    releaseConfig = [
            "group"      : "com.infinum.sentinel",
            "version"    : "$major.$minor.$patch",
            "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}