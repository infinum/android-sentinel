package com.infinum.sentinel.data.models.raw

enum class AppInfo {
    NAME,
    VERSION_CODE,
    VERSION_NAME,
    FIRST_INSTALL,
    LAST_UPDATE,
    MIN_SDK,
    TARGET_SDK,
    PACKAGE_NAME,
    PROCESS_NAME,
    TASK_AFFINITY,
    LOCALE_COUNTRY,
    LOCALE_LANGUAGE
}
